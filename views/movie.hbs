<h2>{{movie.title}}</h2>
    <h3>Director: {{movie.director}}</h4>
    <img src="{{ movie.image }}" alt="">
    <p>{{movie.description}}</p>
    <h4>Stars</h5>
    {{#each movie.stars}}
      <li>{{this}}</li>
    {{/each}}
   